import 'package:flutter/material.dart';

///
class GWDropdown extends StatelessWidget {
  ///
  const GWDropdown({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
